{
  "name": "simple-service-workers-tutorial",
  "version": "1.0.0",
  "description": "Going through the basic tutorial for service workers",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "serve ./",
    "nodemon": "nodemon -e js --no-clipboard --exec serve ./"
  },
  "keywords": [
    "service workers",
    "mdn tutorial"
  ],
  "author": "hychan48",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
